---
- name: Hello World Sample
  hosts: all
  gather_facts: yes
  #vars:
  #  connectivity_test_destinations:
  #    - { ip: 192.168.50.2, port: 53, protocol: udp }
  vars_files:
     - test-vault-1.yml
  #roles:
  #  - ryandaniels.connectivity_test
  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World! - use fact cache: {{ ansible_facts['default_ipv4']['address'] }} total mem: {{ ansible_facts['memtotal_mb'] }} - {{ testpassword }}"
...
